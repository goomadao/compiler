const a=11,b=12;
var c,d,e,i,k;
procedure get;
    const f=34,g=24;
    var h,i;
    procedure put;
        const j=85,k=16;
        var l,m; 
        write(l);
    read(i);
begin
    call get;
    if i<>6 then
        write(i)
    else
        begin
            i := i + 1;
            write(i)
        end
;
    while i > 10
    do
        begin
            k := 6 * i;
            i := i + i;
            write(i)
        end
end.